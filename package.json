{
  "name": "ginraide-project",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --no-package-lock && npm run build && cd ../backend && npm install --no-package-lock && npx prisma generate"
  }
}
